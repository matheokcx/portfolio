export type Skill = {
    name: string,
    icon: string,
    category: string
};

export type Skills = Skill[];


export type Project = {
    title: string,
    image: string,
    link: string,
    stack: string[]
};

export type Projects = Project[];

